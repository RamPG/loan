import Slider from "./Slider";

export default class SecondarySlider extends Slider {
    constructor(pageSelector, nextButtonSelector, prevButtonSelector) {
        super(pageSelector, nextButtonSelector, prevButtonSelector);
    }
}